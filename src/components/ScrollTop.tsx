import { useEffect, useState } from 'react'; import { ArrowUp } from 'lucide-react'
export function ScrollTop(){const [show,setShow]=useState(false);useEffect(()=>{const f=()=>setShow(scrollY>500);addEventListener('scroll',f);return()=>removeEventListener('scroll',f)},[]);if(!show)return null;return <button aria-label="Scroll to top" onClick={()=>scrollTo({top:0,behavior:'smooth'})} className="fixed bottom-6 right-6 z-40 rounded-full bg-berry p-3 text-white shadow-pink transition hover:scale-105"><ArrowUp size={20}/></button>}
