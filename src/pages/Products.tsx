import { useMemo, useState } from 'react'; import { Search, SlidersHorizontal } from 'lucide-react'; import { useSearchParams } from 'react-router-dom'; import { products } from '../data/products'; import { ProductCard } from '../components/ProductCard'; import { Seo } from '../components/Seo'
export default function Products(){const [params]=useSearchParams();const [search,setSearch]=useState('');const [category,setCategory]=useState(params.get('category')||'Todas');const [occasion,setOccasion]=useState('Todas');const [stock,setStock]=useState(false);const filtered=useMemo(()=>products.filter(p=>(category==='Todas'||p.category===category)&&(occasion==='Todas'||p.occasion===occasion)&&(!stock||p.inStock)&&p.title.toLowerCase().includes(search.toLowerCase())),[search,category,occasion,stock]);return <><Seo title="Productos"/><section className="shell section"><div className="rounded-3xl bg-gradient-to-r from-pink-100 via-white to-cyan-100 p-7 sm:p-10"><p className="eyebrow bg-white">Tienda</p><h1 className="heading">Que empiece la celebración.</h1><p className="copy">Artículos premium, bellamente coordinados y listos para tu próximo gran momento.</p></div><div className="mt-8 grid gap-3 rounded-3xl border border-cyan-100 bg-white p-4 shadow-cyan md:grid-cols-4"><div className="relative md:col-span-2"><Search className="absolute left-3 top-3 text-muted" size={19}/><input aria-label="Buscar productos" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Busca la magia de tu fiesta" className="field mt-0 pl-10"/></div><select aria-label="Filtrar por categoría" value={category} onChange={e=>setCategory(e.target.value)} className="field mt-0"><option>Todas</option>{[...new Set(products.map(p=>p.category))].map(x=><option key={x}>{x}</option>)}</select><select aria-label="Filtrar por ocasión" value={occasion} onChange={e=>setOccasion(e.target.value)} className="field mt-0"><option>Todas</option>{[...new Set(products.map(p=>p.occasion))].map(x=><option key={x}>{x}</option>)}</select><label className="flex items-center gap-2 px-2 text-sm font-semibold md:col-span-4"><input type="checkbox" checked={stock} onChange={e=>setStock(e.target.checked)} className="h-4 w-4 accent-berry"/> Solo disponibles <SlidersHorizontal className="ml-auto text-cyan" size={17}/></label></div><p className="mt-6 text-sm text-muted">Mostrando {filtered.length} productos para celebrar</p><div className="mt-5 grid gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{filtered.map(p=><ProductCard key={p.id} product={p}/>)}</div>{!filtered.length&&<p className="card mt-6 bg-butter/10 p-8 text-center text-muted">No encontramos resultados. Prueba con otro filtro.</p>}</section></>}
